# Shared Sources

This directory is the canonical source for cross-platform assets.

- `agents/*.yaml` contains role metadata and base instructions.
- `skills/*.yaml` contains shared skill protocol definitions.

Platform-specific files for OpenCode, Claude Code, and Codex are generated from these sources.
